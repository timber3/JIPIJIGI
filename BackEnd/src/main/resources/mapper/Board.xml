<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jpjg.Ji_Pi_Ji_Gi.board.Mapper.BoardMapper">

    <insert id="writeBoard" parameterType="boardDto">
        insert into board(board_user_id, board_title, board_content) values ('${userId}', '${boardTitle}', '${boardContent}')
    </insert>

</mapper>